name: Hugging Face Nightly Factory Rebuild

on:
  schedule:
    - cron: "0 3 * * *"   # ogni giorno alle 03:00
  workflow_dispatch:

concurrency:
  group: hf-space-rebuild
  cancel-in-progress: true

jobs:
  rebuild:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Factory Rebuild su HF Space
        run: |
          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.HF_TOKEN }}" \
            "https://huggingface.co/api/spaces/frankuaku/bder_rat/restart?factory=true"
